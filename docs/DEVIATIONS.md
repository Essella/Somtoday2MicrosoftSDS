# Known implementation deviations

This register is the single source for known differences between confirmed intent and current implementation. The **Current behavior** column contains code-observed facts; the linked contract contains confirmed intent. IDs are stable: do not reuse or renumber them. A deviation is a known implementation gap, not an unanswered requirement. Resolved IDs remain recorded separately and must not be reused.

## Active deviations

None.

## Resolved deviations

| ID | Resolved | Resolution |
|---|---|---|
| `DEV-034` | 2026-08-07 | Somtoday client secrets are now treated as opaque values: whitespace-only input remains invalid, while every other configured value is passed to authentication without trimming. |
| `DEV-033` | 2026-08-07 | Dedicated Somtoday authentication, public-discovery, and authenticated-data clients now disable automatic redirects, and every 3xx response fails without contacting its target. |
| `DEV-032` | 2026-08-06 | Validation polling now applies a five-second minimum to retries within a poll while retaining longer `Retry-After` values, bounded attempts, cancellation, and the thirty-minute deadline. |
| `DEV-031` | 2026-08-06 | Configuration documentation and regression tests now record the intentional first-character Somtoday environment selection; runtime behavior remains unchanged and complete words remain the readable configuration convention. |
| `DEV-030` | 2026-08-06 | Username startup validation now validates template structure and compiles and caches teacher and pupil formatters without executing them against synthetic records; literal prefixes, suffixes, split, replace, conditionals, and chained string operations are covered. |
| `DEV-029` | 2026-08-06 | Configuration and connector-response validation now reject `Guid.Empty` for institution, inbound-flow, and connector identifiers. |
| `DEV-028` | 2026-08-06 | Separate Graph and SAS publication clients now disable automatic redirects; 3xx responses remain visible to the existing permanent protocol-failure boundary. |
| `DEV-027` | 2026-08-06 | Publication now carries guardian-set state and rejects a dataset that does not contain exactly the required core and guardian files before requesting an SDS upload session. |
| `DEV-026` | 2026-08-06 | Permanent Blob publication and Power Automate handoff were replaced by one connector-selected complete dataset uploaded directly through a fresh Graph SDS SAS session and followed by connector validation. Deployment now creates one system-identity Job per inbound flow and can reuse an existing ACA environment. |
| `DEV-022` | 2026-08-02 | Staging now uses one shared `{Output:Folder}/.staging/{RunId}/{FileName}` root, reserves `.staging` as the first live segment, retains legacy cleanup recognition, excludes staging from rollback, and has bounded base/version lifecycle retention without tags. |
| `DEV-023` | 2026-08-02 | Central CSV serialization now rejects CR and LF in every actively mapped string field before staging, with a safe SDS-version/file/column-only error. |
| `DEV-024` | 2026-08-06 | Generic Host configuration no longer consumes command-line arguments, and header-only mode now relies only on the fixed July 31 trigger in `Europe/Amsterdam`. |
| `DEV-025` | 2026-08-02 | Shared guardian resolution now requires trimmed initials and surname, applies one trimmed name mapping to both SDS versions, omits all rejected references, and exposes only a count for once-per-location logging. |
| `DEV-019` | 2026-08-01 | Complete rollback groups now retain valid versionless base Blobs as no-op restore sources while overwritten files continue to restore from Blob versions. |
| `DEV-020` | 2026-08-01 | Blob service URI validation now accepts only absolute HTTPS URIs in every environment; Development HTTP emulators remain supported through connection strings. |
| `DEV-021` | 2026-08-01 | Blank location abbreviations selected by an empty inclusion list now reach layout validation and fail only their institution instead of being silently omitted. |
| `DEV-014` | 2026-08-01 | Exact trailing staging-path and producer-metadata recognition protects live output paths containing `.staging`; invalid, missing, non-v7, and nested run paths remain untouched. |
| `DEV-015` | 2026-08-01 | Guardian phone normalization now emits only exact ASCII E.164 or an empty value in both SDS versions without removing the guardian or relationships. |
| `DEV-016` | 2026-08-01 | NIGHTLY is now rejected outside Development and removed from both the Bicep production parameter and its regenerated ARM template. |
| `DEV-017` | 2026-08-01 | Authentication now returns an internal transient/permanent result, makes at most four total attempts, retries only network/HTTP timeout, 408, 429, and 5xx failures, and preserves cancellable two-second waits. |
| `DEV-018` | 2026-08-01 | Startup and dataset staging cleanup now make four complete best-effort attempts, try every applicable Blob, preserve primary publication/rollback/cancellation behavior, and warn without changing successful publication or run continuation. |
| `DEV-006` | 2026-08-01 | Complete in-memory datasets are staged once, promoted by server-side copy with one attempt plus three complete-set retries, and restored from the newest older complete application-authored Blob-version group after exhausted promotion. Missing or failed rollback stops later datasets. |
| `DEV-007` | 2026-08-01 | Guardian-enabled datasets always publish their guardian-specific files, including header-only files, while guardian-disabled promotion removes the known guardian files and rollback restores the selected older set's guardian state. |
| `DEV-003` | 2026-07-31 | Both output-layout settings now plan the four confirmed dataset scopes, including location-only collision disambiguation and grouped location conversion. |
| `DEV-004` | 2026-07-31 | Each run now retrieves the institution list once from the unauthenticated public production endpoint and uses the matching public abbreviation independently of the configured synchronization environment. |
| `DEV-001` | 2026-07-30 | The release workflow publishes only the supported `linux/amd64` container image and no Windows archive. |
| `DEV-002` | 2026-07-30 | Releases no longer depend on the `OPENAPI_REDISTRIBUTION_CONFIRMED` repository variable. |
| `DEV-005` | 2026-07-31 | The adapter returns every selected location, normal mode resolves population once and omits locations without an exportable class, and only a publication scope with no eligible location is skipped without changing existing output. Header-only mode remains independent. |
| `DEV-008` | 2026-07-31 | V1 and V2.1 now consume one resolved population, so unassigned employees and pupils are absent from users, roles, enrollments, and other exported files. |
| `DEV-009` | 2026-07-31 | Class membership is now resolved by UUID before eligibility is decided; blank-named classes are excluded, unresolved references are ignored, and every included class retains at least one resolved teacher and pupil. |
| `DEV-010` | 2026-07-30 | Each run captures one `Europe/Amsterdam` date for the July 31 trigger and both converters' school-year calculation. |
| `DEV-011` | 2026-07-30 | V2.1 guardian users include the confirmed given name, family name, and email mappings. |
| `DEV-012` | 2026-07-30 | V1 guardian first and last names follow the confirmed initials and joined-prefix mapping. |
| `DEV-013` | 2026-07-30 | Both converters share consent, email, phone-preference, normalization, and secret-number filtering rules. |

## Detailed resolution records for the 2026-08-01 audit

| ID | Intent | Pre-fix behavior and evidence | Impact | Status, chosen action, and verification | Remaining assumption |
|---|---|---|---|---|---|
| `DEV-014` | Delete only exact application-owned run staging. | `DatasetPublisher.IsOwnedStagingBlob` used `Contains("/.staging/")` plus producer metadata. | Configurable live paths could be deleted. | Resolved by exact segment, compact UUIDv7 version/variant, filename, and metadata validation. Regression tests cover real staging, invalid/missing/non-v7 IDs, extra segments, missing metadata, and `.staging` at configurable live path levels. | Older staging conventions may remain. |
| `DEV-015` | Export exact E.164 or empty while retaining guardian data. | Repeated plus signs were stripped for validation and Unicode digits were accepted through `char.IsDigit`. | Optional phones could invalidate complete SDS datasets. | Resolved with an ASCII anchored final validation. Tests cover Dutch and `00` conversion, exact lower/upper length bounds, plus errors, Unicode digits, letters, invalid country code, and equal empty V1/V2.1 output with relationships retained. | Newly rejected source values intentionally produce empty phone output. |
| `DEV-016` | Accept NIGHTLY only in Development and keep it out of production infrastructure parameters. | Runtime configuration accepted NIGHTLY in every environment, and Bicep and generated ARM exposed it while the endpoint uses HTTP. | A production job could send bearer-authenticated school data over plaintext HTTP. | Resolved by the environment guard and by removing NIGHTLY from Bicep. The ARM template was regenerated and the Bicep source and parameter file were compiled with the official standalone Bicep CLI v0.45.15; tests cover Production rejection and Development acceptance. | Local Development remains responsible for using no real personal data with NIGHTLY. |
| `DEV-017` | Retry transient authentication only, four total attempts. | A 20-retry loop treated every unsuccessful connection alike. | Permanent failures were delayed and transient retry was excessive. | Resolved with `Succeeded`, `TransientFailure`, and `PermanentFailure` outcomes. Tests cover 400/401, 408/429/5xx, network failure, timeout, later recovery, invalid payload, delay duration, and cancellation. | Somtoday data-endpoint retries remain outside scope. |
| `DEV-018` | Make cleanup four-attempt best effort without changing primary status. | Startup tried once, dataset Blobs once each, and Azure deletion stopped on the first failure. | More staging remained and cleanup failure semantics were not protected. | Resolved by complete retries, per-attempt aggregation, and explicit preservation of an in-flight primary exception. Tests cover later success, exhausted startup and dataset warnings, all known Blobs after an individual failure, publication success, primary rollback failure, and preservation of the original cancellation exception. | Staging can remain until a later startup or lifecycle deletion; overlapping runs remain unsupported. |
| `DEV-019` | Inspect both base Blobs and versions and restore the newest older complete application set. | `AzureBlobPublicationStore.GetVersionsAsync` discarded every item without a version ID, and the in-memory fake always assigned a version ID to current Blobs. | A valid older set containing an untouched versionless base Blob could appear incomplete, turning recoverable promotion failure into a fatal rollback failure. | Resolved with restore sources that may have an optional version ID. Azure enumeration retains versionless bases; exact live bases are no-op restores, while overwritten files restore from versions. Tests cover base-only and mixed base/version recovery after partial promotion. | Production still requires Blob versioning so an overwritten base becomes a historical version; no live Azure rollback test was performed. |
| `DEV-020` | Authenticate configured Blob service endpoints only over HTTPS. | `SyncConfiguration.TryCreate` accepted absolute HTTP service URIs, but `BlobClientFactory` always paired them with `DefaultAzureCredential` and the Azure SDK rejected that combination later. | Configuration validation reported an unusable setup as valid and failed only during client construction. | Resolved by rejecting every non-HTTPS service URI during configuration creation. Tests cover both Production and Development rejection, existing HTTPS identity precedence, and the Development connection-string path. | Local Azurite over HTTP remains intentionally connection-string-only. |
| `DEV-021` | A selected location without a usable abbreviation fails only its institution. | `LocationSelector` silently removed blank-abbreviation locations before output planning. | An incomplete source location list could be published without the operator-visible institution failure required for invalid layout input. | Resolved by retaining blank codes when the inclusion list is empty and using existing layout validation as the institution failure boundary. Tests cover selection, explicit inclusion, and all four grouping combinations while preserving another institution. | No fallback folder name is inferred; a non-empty inclusion list cannot select a blank code. |

## Detailed resolution records for the 2026-08-02 publication changes

| ID | Intent | Pre-fix behavior and evidence | Impact | Status, chosen action, and focused verification | Remaining assumption |
|---|---|---|---|---|---|
| `DEV-022` | Keep all staging directly below the normalized output folder, make it disposable, and bound its infrastructure retention without tags. | `DatasetPublisher` built staging from each `livePrefix`, `OutputLayoutPlanner` did not reserve a first `.staging` live segment, and `infra/main.bicep` had only the general seven-day previous-version rule. | Staging was split across live V1/V2.1 scopes, lifecycle cleanup could not target one bounded prefix, and old base staging could persist indefinitely. | Resolved by passing the normalized output prefix into `DatasetPublisher`, staging sequential datasets at the shared root and overwriting reused names, reserving the first relative `.staging` segment case-insensitively, recognizing current and legacy cleanup tails, and keeping rollback limited to live bases and versions. Bicep now derives one normalized runtime/lifecycle prefix and makes staging bases and versions eligible after more than one day with a block-Blob path filter and no tags. Focused publisher, layout, and infrastructure assertions cover exact paths, metadata, complete V2.1-set overwrite after cleanup exhaustion, lookalikes, legacy cleanup, cancellation, rollback exclusion, lifecycle actions, and retained seven-day policies. | Azure lifecycle processing remains asynchronous; soft delete can retain deleted staging temporarily, and overlapping application runs remain unsupported. |
| `DEV-023` | Reject CR or LF in every string field represented on the SDS wire before any upload. | `FileHelper.SerializeCsv` passed records directly to `CsvWriter.WriteRecord`; CsvHelper could quote embedded newlines, but there was no SDS-specific wire validation. | A syntactically serializable file could violate the confirmed SDS single-line field contract, and the failure would not identify the safe file/column boundary. | Resolved by inspecting only string members in the active `ClassMap` before each record write and throwing `InvalidDataException` with SDS version, exact file, and exact header only. Focused wire tests cover every writable mapped string column with `\r`, `\n`, and `\r\n`, safe messages, unmapped matching-only name properties, header-only output, exact file/header casing, UTF-8 without BOM, zero staging, unchanged live state, and continuation with the other version. | This is wire-shape validation, not full external SDS service validation. |
| `DEV-024` | Keep configuration in documented providers only and remove operator overrides for header-only mode while retaining the fixed July 31 trigger. | `Program` called `Host.CreateApplicationBuilder(args)`, which implicitly registered command-line configuration and allowed undocumented overrides, and application logic still accepted a separate runtime argument override for header-only mode. | Operators could bypass documented provider precedence and trigger header-only mode through an undocumented runtime override. | Resolved by creating the Generic Host without `args`, removing separate runtime-argument handling, and removing the legacy output setting for header-only mode. Focused tests assert that the command-line provider is absent and that header-only mode depends only on the fixed year-end date. | JSON, environment-specific JSON, Development User Secrets, and environment variables retain their existing order. |
| `DEV-025` | Export a guardian only with contact consent/email, a relevant included pupil, and usable trimmed initials and surname. | `ExportPopulationResolver` filtered contact and pupil relationship but did not reject null, empty, or whitespace `Voorletters` or `Achternaam`; converters could therefore write guardians with names SDS requires but the source did not supply. | Invalid guardians and their roles/relationships could reach either SDS format, while operators had no privacy-safe indication of name-only exclusions. | Resolved by one shared guardian policy and resolved-population filter, common trimmed given/family-name mapping, consistent removal from V1 and V2.1 user/role/relationship records, and a name-only exclusion counter whose log contains only the count once per location. Focused resolver, aggregation, and conversion tests cover null/empty/whitespace initials and surname, prefix-only names, relevant relationships, exact trimming, safe counts, consistent omission, and header-only guardian files. | Guardians with an invalid optional phone remain included with an empty phone value. |

## Detailed resolution record for direct SDS publication

| ID | Intent | Pre-fix behavior and evidence | Impact | Status, chosen action, and focused verification | Remaining assumption |
|---|---|---|---|---|---|
| `DEV-026` | Publish one complete connector-selected dataset directly to SDS for one inbound flow. | The application always produced both formats, staged them in permanent Azure Blob Storage, and required an operator-owned Power Automate handoff. Infrastructure provisioned Storage and supported output grouping rather than one Job per inbound flow. | Publication was indirect, retained personal data, duplicated formats, and could not be deployed repeatedly into one shared ACA environment for distinct inbound flows. | Resolved by `InboundFlowId` configuration, connector relation lookup, format selection, in-memory complete-set construction, fresh SAS upload session, exact sequential Put Blob transport, Graph validation polling, successful-school subset behavior, system-assigned identity Graph app-role assignments, and `new`/`existing` environment modes. Focused tests cover endpoint and ID separation, formats, SAS query and headers, bearer isolation, failure-before-validation, polling, and `Retry-After`. | Microsoft Graph industry-data endpoints remain `/beta`; no authenticated tenant ingest was performed during implementation. |

## Detailed resolution records for the 2026-08-06 conformance follow-up

| ID | Intent | Pre-fix behavior and evidence | Impact | Status, chosen action, and focused verification | Remaining assumption |
|---|---|---|---|---|---|
| `DEV-028` | Do not follow redirects during Graph or SAS publication. | `Program` used default `IHttpClientFactory` clients whose primary handlers followed redirects before `SdsGraphClient` could require the exact operation status. | A publication request could leave the exact Graph endpoint or SAS container URL selected for the run. | Resolved with separate named Graph and SAS clients whose handlers set `AllowAutoRedirect=false`. Tests cover handler configuration, Graph 302/307, SAS 302/307, one-attempt failure, no redirect-target request, and no validation after a redirected upload. | Somtoday redirects were addressed separately by `DEV-033`. |
| `DEV-029` | Accept only non-empty institution, inbound-flow, and connector UUIDs. | `Guid.TryParse` accepted the all-zero value in `SyncConfiguration` and `SdsGraphClient.GetConnectorAsync`. | Invalid identities passed their earliest validation boundary. | Resolved by explicit `Guid.Empty` rejection. Tests cover empty school, inbound-flow, and connector identifiers while retaining valid ordering and duplicate checks. | No compatibility is retained for an all-zero identifier. |
| `DEV-030` | Support fast compiled IDM templates with literal text and supported Dynamic LINQ string operations. | Template normalization rejected some literal prefixes and suffixes, and startup validation invoked formatters against one synthetic object. | Valid rules could be rejected because of irrelevant sample data, while execution performance depends on compiled formatter reuse. | Resolved by structural brace validation and startup compilation into the existing concurrent teacher and pupil caches without invocation. Dataset construction reuses those delegates. Tests cover prefixes, suffixes, multiple expressions, `Split`, `Replace`, `ToLower`, chaining, conditionals, data-dependent compile-only validation, unknown members, invalid methods, and unmatched braces. | Bare values remain property shorthand; data-dependent evaluation errors still fail dataset construction before upload. |
| `DEV-031` | Select the Somtoday environment by its first non-whitespace character and use complete words for readable configuration. | Runtime already implemented this rule, but operator documentation did not distinguish it from accidental permissiveness. | The supported configuration behavior was unclear. | Resolved without runtime changes. The configuration guide and README record the decision, and regression tests cover trimmed first-character selection and the Development-only `N` boundary. | Bicep and tracked examples continue to use complete environment names. |
| `DEV-032` | Keep all consecutive validation HTTP attempts at least five seconds apart. | A logical poll waited five seconds, but its internal retry could honor `Retry-After: 0` and run immediately. | Validation could exceed the agreed poll frequency. | Resolved with an optional retry minimum applied only to validation polling. Tests prove a zero retry value becomes five seconds and a longer value remains unchanged. Existing cancellation, attempt, and timeout tests remain green. | The minimum applies to HTTP attempts, not only completed logical polls. |

## Detailed resolution records for the 2026-08-07 confirmed behavior changes

| ID | Intent | Pre-fix behavior and evidence | Impact | Status, chosen action, and focused verification | Remaining assumption |
|---|---|---|---|---|---|
| `DEV-033` | Keep every Somtoday request on its configured endpoint and treat redirects as permanent protocol failures. | `OpenAPIHelper` used unnamed `IHttpClientFactory` clients with automatic redirects enabled for authentication, public discovery, and authenticated data reads. | A redirect could move a request away from the configured Somtoday endpoint; a method-preserving authentication redirect could also carry the authentication form to its target. | Resolved with three dedicated no-redirect clients. Tests inspect every configured primary handler, verify the dedicated client selection, and cover 302, 307, and 308 as one-attempt permanent failures. | No supported Somtoday endpoint requires redirects. An endpoint change requires an application update instead of a runtime redirect. |
| `DEV-034` | Treat the Somtoday client secret as an opaque configuration value. | `SyncConfiguration.TryCreate` trimmed the configured secret after rejecting null, empty, or whitespace-only input. | A valid secret with significant leading or trailing whitespace could not be authenticated exactly as configured. | Resolved by retaining the original non-whitespace-only value. Tests cover exact preservation and continued rejection of whitespace-only input. | Operators remain responsible for supplying the intended exact secret value. |

## Audit evidence

The 2026-07-30 audit inspected solution/project files, startup, configuration, dependency injection, helpers, CSV models, tests, Dockerfile, Bicep, scripts, workflows, Markdown, generated-client headers, OpenAPI metadata, and recent Git history.

It did not inspect every generated line of `openapi.cs`, every schema entry in `openapi.json`, or generated ARM JSON. `dotnet test .\Somtoday2MicrosoftSDS.sln --configuration Release` passed 38 tests with no failures or skips.

The audit read the public Somtoday institution endpoint and official Microsoft documentation. It did not access an authenticated Somtoday environment, Azure subscription, production Blob data, or Microsoft SDS tenant.

The 2026-08-01 remediation audit restored the solution and ran the complete Release test suite: 128 tests passed and the seven credential-dependent live Somtoday tests were skipped. The Release build with warnings treated as errors succeeded with zero warnings, and `git diff --check` succeeded. The OpenAPI specification and generated client were unchanged. The official standalone Bicep CLI v0.45.15 compiled `infra/main.bicep` and `infra/main.example.bicepparam`, regenerated `infra/azuredeploy.json`, and confirmed that NIGHTLY is absent from the production Bicep and ARM parameter. No authenticated Somtoday, Azure-subscription, production Blob, or Microsoft SDS ingest test was performed.

The 2026-08-01 follow-up remediation restored both the solution and the `linux-x64` runtime assets. The complete Release suite passed 138 tests and skipped the same seven credential-dependent live Somtoday tests. The warnings-as-errors Release build succeeded with zero warnings, the framework-dependent `linux-x64` publish succeeded, and a repository Markdown check validated 64 relative link targets. `git diff --check` succeeded. The OpenAPI specification, generated client, and infrastructure files remained unchanged. No authenticated Somtoday, live Azure Blob rollback, production Blob, container, or Microsoft SDS ingest test was performed.

The 2026-08-02 publication remediation ran the complete Release suite: 175 tests passed and the same seven credential-dependent live Somtoday tests were skipped. The warnings-as-errors Release build succeeded with zero warnings, and the framework-dependent `linux-x64` publish succeeded. Official standalone Bicep CLI v0.45.15 compiled `infra/main.bicep` and `infra/main.example.bicepparam`; canonical comparison confirmed that regenerated `infra/azuredeploy.json` matches the Bicep source. The repository Markdown check found no missing relative targets across all 15 tracked Markdown files. The Power Automate template image was visually checked after privacy-safe replacement, and metadata inspection found no EXIF, IPTC, or XMP data. `git diff --check` succeeded. The OpenAPI specification and generated client were unchanged. No authenticated Somtoday, Azure-subscription, live Blob rollback, production Blob, Power Automate flow, or Microsoft SDS ingest test was performed.

The 2026-08-06 direct SDS publication change restored the solution and `linux-x64` runtime assets, built with warnings as errors and zero warnings, passed 115 tests, and skipped the seven credential-dependent live Somtoday tests. The framework-dependent `linux-x64` publish and the complete WSLC container smoke test succeeded, including non-root execution, missing-configuration failure, and SIGTERM cancellation during managed-identity token acquisition. Official standalone Bicep CLI v0.45.15 compiled `infra/main.bicep`, both parameter examples, and regenerated `infra/azuredeploy.json` with the Microsoft Graph extension. The Somtoday OpenAPI specification and generated client were unchanged. No authenticated Somtoday, Azure subscription deployment, Microsoft Entra role assignment, or live Microsoft SDS upload/validation was performed.

The 2026-08-06 conformance follow-up restored the solution and `linux-x64` runtime assets, built with warnings as errors and zero warnings, passed 137 tests, and skipped the seven credential-dependent live Somtoday tests. The framework-dependent `linux-x64` publish succeeded, repository Markdown links resolved, and `git diff --check` succeeded. The Somtoday OpenAPI artifacts and infrastructure were unchanged. No authenticated Somtoday, Azure subscription, or live Microsoft SDS upload/validation was used.

The 2026-08-07 confirmed behavior change passed 145 tests and skipped the seven credential-dependent live Somtoday tests. The warnings-as-errors Release build succeeded with zero warnings, the framework-dependent `linux-x64` publish succeeded, all relative links across the 15 tracked Markdown files resolved, and `git diff --check` succeeded. The Somtoday OpenAPI artifacts and infrastructure were unchanged. No authenticated Somtoday, Azure subscription, or live Microsoft SDS upload/validation was used.
